import React from 'react';
import classes from './BandInterface.module.css';

const BandInterface = props => {
    return (
        <div>
            Interface for a band!
        </div>
    );
};

export default BandInterface;