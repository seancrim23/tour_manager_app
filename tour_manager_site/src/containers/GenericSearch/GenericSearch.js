import React from 'react';
import classes from './GenericSearch.module.css';

const GenericSearch = props => {
    return (
        <div className={classes.GenericSearch}>
            Search for shows in your area!
        </div>
    );
};

export default GenericSearch;